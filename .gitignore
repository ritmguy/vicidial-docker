.DS_Store
.env

# Real DB creds — create from docker/mysql/mysql.env.example
docker/mysql/mysql.env

# Throwaway GitHub-release bodies — generated per release, never committed (see RELEASING.md)
release-notes/

# Maintainer-only — not shared with collaborators/testers
CLAUDE.local.md
RELEASING.md

# Brainstorming specs & implementation plans — private, never committed
docs/superpowers/

# Agent run scratch — not shared, not committed
.superpowers/
